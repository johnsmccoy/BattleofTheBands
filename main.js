console.log("Battle Time")


let battle = 0

let takeNumber = function (name) {
    bandNumber +=1
   return bandNumber + ". " + name
}

const train = takeNumber("Train")
console.log(train)  // This should print "Train" in the console

const drake = takeNumber("Drake")
console.log(drake)  // This should print "Drake" in the console